# 人力资源发薪 SaaS 平台完整技术方案
（技术栈：Node.js + NestJS + MySQL + Redis + 微信小程序）

## 开发阶段划分

> ⚠️ 本项目分为两个阶段开发：
>
> **第一阶段（当前）**：登录权限、企业管理、项目管理、员工管理、账户管理（充值）
> - 不包含发薪模块
> - 不包含小程序端（员工可通过 Web 端查看信息）
>
> **第二阶段**：发薪引擎模块、微信小程序端

---
开发一套人力资源发薪saas平台，主要用户角色是：服务商（平台管理员）、特约商户（企业）、员工（企业的发薪对象）。主要功能是服务商运营该平台；平台管理员添加维护企业信息和企业管理员信息后，企业通过平台进行项目管理、员工管理、企业自身账户管理、发薪操作；员工完成签约企业后，可通过平台发薪功能获得薪资。  
具体功能列表：  
- 企业管理：公司列表、创建公司和公司管理员、启停公司、查看公司详情和公司联系人、查看公司内部数据（项目、账户余额、充值流水、发薪流水）  。角色权限：平台管理员。
- 企业基本信息：公司账号管理（账号的增删改查和权限配置，包括登录密码和支付密码维护、启停管理）。角色权限：平台管理员、企业管理员
- 项目管理：项目的增删改查；项目包括项目名称、人数、驻厂、财务、项目二维码（项目二维码用于员工注册及资料填报签约）；项目启停。角色权限：企业管理员.
- 员工管理：员工的增删改查；员工信息包括姓名、身份证号、手机号、录入时间、所属项目、状态(待上岗、在职、离职待支付、离职、辞退、开除)、紧急联系人、紧急联系人电话、身份证照片正反面。
- 账户管理：账户余额查询、充值记录、充值、
- 发薪操作：项目发薪（选择项目、选择员工、设置金额）提交发薪、发薪预览、发薪验证（支付密码输入）；发薪流水；发薪日志（发薪列表、时间、操作人）；发薪限额配置（单笔总金额、单笔单人金额）；发薪服务对接(微信微工卡，MVP不做).

---

# 一、项目定位

## 1.1 平台角色

| 角色 | 说明 |
|------|------|
| 服务商（平台管理员） | 负责运营平台，管理企业 |
| 企业管理员 | 管理企业内部项目、员工、发薪 |
| 员工 | 通过微信小程序签约项目并接收薪资 |

---

# 二、总体架构设计

采用：

> ✅ 模块化单体架构（Modular Monolith）  
> ✅ 单数据库 + 逻辑多租户隔离（tenant_id）  
> ✅ 公有云部署（腾讯云/阿里云）  
> ✅ 微信小程序作为员工端  

---

## 2.1 系统架构图（逻辑结构）
客户端层
├─ 平台管理端（Web）
├─ 企业管理端（Web）
└─ 员工端（微信小程序）

后端 API 层（NestJS）
├─ 认证模块
├─ 租户模块
├─ 组织模块
├─ HR 核心模块（公司-项目-员工）
├─ 账户模块
├─ 发薪引擎模块（独立）
└─ 审计日志模块

基础设施层
├─ MySQL 8
├─ Redis
├─ 对象存储（OSS）
└─ Nginx



---

# 三、技术栈说明

## 3.1 后端

| 技术 | 说明 |
|------|------|
| Node.js 20+ | 运行环境 |
| NestJS | 主框架 |
<!-- | TypeORM 或 Prisma | ORM | -->
| TypeORM | ORM |
| MySQL 8 | 主数据库 |
| Redis | 缓存 + Token |
| JWT | 登录认证 |
| bcrypt | 密码加密 |
| class-validator | 参数校验 |

---

## 3.2 前端

### Web 端（平台 + 企业）
- Vue3 + Vite
- Element Plus

### 员工端
- 微信小程序原生开发

---

# 四、多租户设计（逻辑隔离）

采用：

> 单数据库 + tenant_id 字段隔离

---

## 4.1 设计原则

所有核心业务表必须包含：tenant_id BIGINT NOT NULL

所有查询必须自动附加：WHERE tenant_id = 当前企业ID


---

## 4.2 tenant_id 设计说明

> ⚠️ tenant_id = company_id（公司ID）

各表的 tenant_id 字段设计：

| 表名 | tenant_id 说明 |
|------|----------------|
| company | 不需要 tenant_id（自身即为租户） |
| project | 需要 tenant_id，关联所属公司 |
| employee | 需要 tenant_id（员工属于公司，不一定属于某个项目） |
| account | 需要 tenant_id（企业账户） |
| transaction_record | 需要 tenant_id（资金流水） |
| payroll_order | 需要 tenant_id（发薪记录） |
| payroll_detail | 不需要，从 payroll_order 关联 |

**注意**：employee 表保留 tenant_id 是因为员工可能暂时不属于任何项目，但仍属于公司员工。

---

## 4.3 实现方式

在 NestJS 中：

- 使用 TypeORM Subscriber 自动注入 tenant_id
- 创建 TenantSubscriber 类监听 BeforeInsert 事件
- 从请求上下文（Request）获取当前 tenant_id

### 实现示例

```typescript
@EventSubscriber()
export class TenantSubscriber implements EntitySubscriberInterface {
  listenTo() {
    return 'all';
  }

  beforeInsert(event: InsertEvent<any>) {
    const tenantId = getTenantIdFromContext(); // 从请求获取
    if (tenantId && !event.entity.tenant_id) {
      event.entity.tenant_id = tenantId;
    }
  }
}
```

---

# 1️⃣ 租户与企业管理模块

## 功能

- 公司列表
- 创建公司
- 启停公司
- 创建企业管理员
- 查看企业内部数据统计

## 主要表

### company

| 字段 | 说明 |
|------|------|
| id | 主键 |
| tenant_id | 租户ID |
| name | 公司名称 |
| status | 启用/停用 |
| contact_name | 联系人 |
| contact_phone | 联系电话 |
| created_at | 创建时间 |

---

# 2️⃣ 项目与员工模块（HR核心）

## 项目功能

- 创建项目
- 编辑项目
- 项目二维码生成
- 项目启停

### project

| 字段 | 说明 |
|------|------|
| id | 主键 |
| tenant_id | 租户ID |
| name | 项目名称 |
| status | 状态 |
| qr_code_url | 二维码 |
| created_at | 创建时间 |

---

## 员工功能

- 员工CRUD
- 员工状态管理
- 身份证照片上传

### employee

| 字段 | 说明 |
|------|------|
| id | 主键 |
| tenant_id | 租户ID |
| project_id | 所属项目 |
| name | 姓名 |
| id_card | 身份证号 |
| phone | 手机号 |
| status | 在岗/离职等 |
| id_card_front | 正面照片 |
| id_card_back | 背面照片 |
| created_at | 创建时间 |

---

# 3️⃣ 账户与资金模块

### account

| 字段 | 说明 |
|------|------|
| id | 主键 |
| tenant_id | 租户ID |
| balance | 当前余额 |

---

### transaction_record

| 字段 | 说明 |
|------|------|
| id | 主键 |
| tenant_id | 租户ID |
| type | 充值/发薪 |
| amount | 金额 |
| before_balance | 操作前余额 |
| after_balance | 操作后余额 |
| created_at | 时间 |

---

# 4️⃣ 发薪引擎模块（独立模块）

> 这是系统核心，必须独立设计

---

## 4.1 发薪流程状态机
DRAFT
↓
PENDING_VERIFY
↓
PAID
↓
FAILED


---

## 4.2 核心表

### payroll_order

| 字段 | 说明 |
|------|------|
| id | 主键 |
| tenant_id | 租户ID |
| project_id | 项目 |
| total_amount | 总金额 |
| status | 状态 |
| operator_id | 操作人 |
| created_at | 创建时间 |

---

### payroll_detail

| 字段 | 说明 |
|------|------|
| id | 主键 |
| payroll_order_id | 发薪单ID |
| employee_id | 员工ID |
| amount | 金额 |
| status | 状态 |

---

## 4.3 发薪核心逻辑

必须实现：

- 限额校验（单笔总额、单人金额）
- 余额校验
- 支付密码校验
- 幂等控制
- 数据库事务

---

## 4.4 发薪伪代码逻辑
BEGIN TRANSACTION

校验支付密码
校验限额
校验余额

创建发薪单
插入发薪明细
扣减企业余额
写入资金流水
更新发薪状态

COMMIT


---

# 五、数据字典（枚举定义）

## 5.1 员工状态（employee.status）

| 值 | 说明 |
|----|------|
| PENDING | 待上岗（新录入，还未分配项目） |
| ACTIVE | 在职（正常在岗） |
| PENDING_EXIT | 离职待支付（已提交离职，等待最后一笔薪资发放） |
| RESIGNED | 离职 |
| FIRED | 辞退 |
| DISMISSED | 开除 |

## 5.2 项目状态（project.status）

| 值 | 说明 |
|----|------|
| ACTIVE | 启用 |
| INACTIVE | 停用 |

## 5.3 公司状态（company.status）

| 值 | 说明 |
|----|------|
| ACTIVE | 启用 |
| INACTIVE | 停用 |

## 5.4 交易类型（transaction_record.type）

| 值 | 说明 |
|----|------|
| RECHARGE | 充值 |
| PAYROLL | 发薪 |
| REFUND | 退款 |

## 5.5 发薪单状态（payroll_order.status）

| 值 | 说明 |
|----|------|
| DRAFT | 草稿 |
| PENDING_VERIFY | 待验证（等待支付密码确认） |
| PROCESSING | 处理中 |
| PAID | 已完成 |
| FAILED | 失败 |
| PARTIAL | 部分成功 |

## 5.6 账户状态（account.status）

| 值 | 说明 |
|----|------|
| ACTIVE | 正常 |
| FROZEN | 冻结 |

---

# 六、权限模型设计（RBAC）

角色：

- PLATFORM_ADMIN
- COMPANY_ADMIN
- EMPLOYEE

使用 NestJS Guard 实现：

- @Roles('COMPANY_ADMIN')
- @UseGuards(AuthGuard, RolesGuard)

---

# 七、员工微信小程序设计

## 功能

- 微信登录
- 扫码加入项目
- 填写个人信息
- 查看发薪记录

---

## 微信登录流程

1. 小程序调用 wx.login()
2. 后端调用微信接口获取 openid
3. 生成 JWT
4. 绑定 employee 账号

---

# 八、安全设计

## 8.1 密码安全

- 登录密码：使用 bcrypt 加密（salt rounds = 10）
- 支付密码：使用 bcrypt 加密（与登录密码独立，salt rounds = 10）
- 支付密码验证：错误不返回具体错误原因（防暴力破解）

## 8.2 Token 安全

- JWT 有效期：1 小时（access_token）
- Refresh Token：7 天，存储在 Redis
- Redis 存储黑名单（登出时加入）
- 登录失败次数限制：连续 5 次失败锁定 15 分钟
- API 限流：每个 IP 每分钟 60 次

---

# 九、部署方案（腾讯云/阿里云）

## 推荐结构

- 云服务器 ECS（2核4G）
- MySQL RDS
- Redis
- 对象存储 OSS
- Nginx 反向代理

---

## 部署流程

1. Docker 化部署
2. 使用 PM2 或 Node Cluster
3. HTTPS（Let's Encrypt）

---

# 十、API 设计规范

## 9.1 RESTful 规范

| 方法 | 用途 | 示例 |
|------|------|------|
| GET | 查询 | GET /api/companies |
| POST | 创建 | POST /api/companies |
| PATCH | 部分更新 | PATCH /api/companies/:id |
| DELETE | 删除 | DELETE /api/companies/:id |

## 9.2 响应格式

```json
// 成功
{
  "code": 0,
  "message": "success",
  "data": { }
}

// 失败
{
  "code": 1001,
  "message": "错误描述",
  "data": null
}
```

## 9.3 分页格式

```json
{
  "list": [ ],
  "total": 100,
  "page": 1,
  "pageSize": 20
}
```

---

# 十一、错误码体系

## 10.1 系统错误码

| 错误码 | 说明 |
|--------|------|
| 0 | 成功 |
| 1000 | 系统错误 |
| 1001 | 参数错误 |
| 1002 | 资源不存在 |
| 1003 | 权限不足 |
| 1004 | 认证失败 |
| 1005 | Token 过期 |

## 10.2 业务错误码

| 错误码 | 说明 |
|--------|------|
| 2001 | 账户余额不足 |
| 2002 | 支付密码错误 |
| 2003 | 账户已冻结 |
| 2004 | 项目已停用 |
| 2005 | 员工不存在 |
| 2006 | 超出发薪限额 |
| 2007 | 重复提交 |

---

# 十二、日志规范

## 11.1 日志级别

| 级别 | 使用场景 |
|------|----------|
| ERROR | 错误堆栈、异常捕获 |
| WARN | 业务警告（如余额不足） |
| INFO | 关键业务节点（登录、充值、发薪） |
| DEBUG | 开发调试信息 |

## 11.2 日志格式

```
[时间] [级别] [模块] [请求ID] 内容
2024-01-01 10:00:00 INFO [AuthModule] [req-123] 用户登录成功
```

## 11.3 必须记录的日志

- 用户登录/登出
- 充值操作
- 发薪操作（包含金额）
- 敏感数据修改（密码、权限）
- 业务异常

---

# 十三、不做的复杂功能（当前阶段）

> ⚠️ 第一阶段不做：发薪模块、微信小程序

- 微服务拆分
- 自动扩缩容
- 分布式事务
- 消息队列
- 多数据库隔离

---

# 十四、后续可扩展路径

当企业数量增长时：

1. 将发薪模块拆成独立服务
2. 引入消息队列（RabbitMQ）
3. 引入容器化部署（Docker + Kubernetes）
4. 引入读写分离

---

# 十五、开发阶段建议

> ⚠️ 本项目第一阶段不包含发薪模块和小程序端

## 第一阶段（当前开发）

- 登录权限（JWT + RBAC）
- 企业管理（平台管理员）
- 项目管理（企业管理员）
- 员工管理（增删改查、状态管理）
- 账户管理（余额查询、充值）
- 资金流水记录

## 第二阶段

- 发薪引擎模块
- 微信小程序端
- 报表统计

---

# 最终架构总结

当前规模最佳方案：

✅ 模块化单体  
✅ 单库多租户逻辑隔离  
✅ NestJS  
✅ MySQL  
✅ Redis  
✅ 微信小程序员工端  
✅ 公有云部署  

---
